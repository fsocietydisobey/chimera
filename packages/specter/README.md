# specter

Browser debugging via Chrome DevTools Protocol. Console logs, screenshots, React component tree, network capture, page interaction.

Library mode: `from specter.api import connect, screenshot, get_component_tree, ...`
MCP server: `python -m specter.server`

See the [workspace README](../../README.md) for the full vision.
