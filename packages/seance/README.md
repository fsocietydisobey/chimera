# seance

Semantic code search via vector embeddings. Find code by meaning, not just keywords.

Library mode: `from seance.api import semantic_search, find_similar, ...`
MCP server: `python -m seance.server`

See the [workspace README](../../README.md) for the full vision.
