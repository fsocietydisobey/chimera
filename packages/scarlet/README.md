# scarlet

Codebase cartography — generates per-feature CLAUDE.md, dep graphs, barrel exports, symbol manifests via tree-sitter.

Library mode: `from scarlet.api import scan_features, build_claude_md, ...`
MCP server: `python -m scarlet.server`

See the [workspace README](../../README.md) for the full vision.
